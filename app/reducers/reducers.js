/* Actions */
// Import Actions

// Initial state params
const GLOBAL_STATE = { globalVar: [] };

export var globalReducer = (state=GLOBAL_STATE, action) => {
	switch(action.type) {
		default:
			return state;
	}
};
